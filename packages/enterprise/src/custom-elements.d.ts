// Mirrors packages/ui/src/custom-elements.d.ts. This was a git symlink, but
// Windows checkouts without symlink privileges materialize symlinks as plain
// text files, which breaks typecheck. A triple-slash reference keeps the
// single source of truth while remaining checkout-safe everywhere.
/// <reference path="../../ui/src/custom-elements.d.ts" />
